apiVersion: v1
kind: Service
metadata:
    name: andymaintainazuredevopsacraksapp
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: andymaintainazuredevopsacraksapp